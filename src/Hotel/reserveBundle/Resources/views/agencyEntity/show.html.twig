{% extends '::base.html.twig' %}

{% block body -%}
    <h1>agencyEntity</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Agency_active</th>
            <td>{{ entity.agencyactive }}</td>
        </tr>
        <tr>
            <th>Agency_name</th>
            <td>{{ entity.agencyname }}</td>
        </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('agencyentity') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('agencyentity_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
